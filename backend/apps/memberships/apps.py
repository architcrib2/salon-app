"""AppConfig for memberships app."""
from django.apps import AppConfig

class MembershipsConfig(AppConfig):
    name = 'apps.memberships'
    verbose_name = 'Memberships'
    default_auto_field = 'django.db.models.BigAutoField'
